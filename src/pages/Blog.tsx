// Packages
import React from "react";

// Components
import { MainCard } from "../components";

// Styles
import { Wrapper } from "./styles";

const Blog: React.FC = () => {
  // -------------------------------------------------
  // Render
  // -------------------------------------------------
  return (
    <Wrapper>
      <MainCard />
    </Wrapper>
  );
};

export default Blog;
