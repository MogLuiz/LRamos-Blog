import { styled } from "../../styles";

export const Container = styled("div", {
  margin: "100px", // Retirar
});

export const ImagePost = styled("img", {
  width: "600px",
  height: "400px",

  borderRadius: "20px"
});
